# سجل التغييرات (Changelog)

ملخص لأهم مراحل تطوير المتجر. لتفاصيل تقنية أعمق راجع
[`WAMEED-STORE-DOCS.md`](./WAMEED-STORE-DOCS.md).

## قاعدة البيانات: الانتقال من SQLite/Turso إلى PostgreSQL

- تم تبسيط قاعدة البيانات بالكامل إلى **PostgreSQL** (عبر تبويب Storage المدمج
  في Vercel، بدون حاجة لحساب أو موقع خارجي أو نسخ توكنات يدوياً).
- إزالة الاعتماد على SQLite المحلي و Turso/libSQL بالكامل من `src/lib/db.ts`
  ومن `prisma/schema.prisma` (`provider = "postgresql"`).
- سبب هذا التغيير: SQLite المحلي لا يعمل على استضافة Vercel (نظام ملفات غير
  دائم)، بينما كانت خطوات ربط Turso تتطلب تعاملاً يدوياً مع التوكنات عبر
  الطرفية (CLI) وهو ما سبب صعوبة للمستخدمين غير التقنيين.

## جاهزية الإطلاق والاستضافة

- إصلاح إعداد `next.config.ts` (`output: "standalone"`) المطلوب لسكربتات
  `build`/`start` عند الاستضافة الذاتية.
- إصلاح `prisma/schema.prisma` (`previewFeatures = ["driverAdapters"]`)
  المطلوب لعمل قاعدة Turso في الإنتاج.
- إزالة نقطة API غير محمية كانت تسمح بتحميل كامل الكود المصدري بدون تسجيل دخول.
- توحيد أسماء متغيرات البيئة وتوثيقها بالكامل في `.env.example`.
- استبدال الاعتماد على `bun` في السكربتات بـ `node`/`tsx` لتوافق أوسع.
- تشديد حماية CSRF في `middleware.ts` (إزالة استثناء كان يُبطل الفحص فعلياً
  خلف أي بروكسي عكسي، وهي الحالة الشائعة في أغلب بيئات الاستضافة).
- إضافة صلاحية زمنية فعلية لجلسة تسجيل دخول الأدمن (7 أيام) بدل الاعتماد على
  توقيع بلا انتهاء صلاحية.
- تنظيف المستودع من ملفات بيئة التطوير الداخلية (لقطات شاشة، سجلات، أرشيفات
  مكررة) وإعادة تهيئة Git بسجل نظيف.

## الأمان والجودة

- إزالة كلمة مرور الأدمن من كود الواجهة الأمامية.
- استبدال التجزئة الضعيفة بـ HMAC-SHA256 + مقارنة زمنية ثابتة + كوكي Secure.
- تجزئة كلمة المرور بـ scrypt مع ترقية تلقائية لأي كلمة مرور نصية قديمة.
- تحديد معدل محاولات تسجيل الدخول (5 محاولات كل 15 دقيقة).
- التحقق من صحة جميع طلبات الإدارة عبر Zod.
- حماية CSRF عبر فحص Origin/Referer على الطلبات المغيّرة للبيانات.
- تحسين السيو: `robots.txt`، `sitemap.ts`، Open Graph، Twitter metadata.

## الميزات

- نظام مراجعات وتقييمات للمنتجات.
- الدفع بالبطاقة عبر Stripe (بالإضافة إلى الدفع عند الاستلام عبر واتساب).
- نظام إشعارات بريد إلكتروني (تأكيد الطلب، تحديث الحالة) بقوالب HTML عربية.
- دعم قاعدة بيانات Turso/libSQL السحابية للإنتاج (تمت إزالته لاحقاً، راجع
  الملاحظة أسفل القسم).
- إعداد نطاق مخصص عبر Vercel (راجع قسم النشر في `README.md`).
